# This is a basic workflow to help you get started with Actions

name: Reusable Workflow 2

on:
  workflow_dispatch:

jobs:
  reusable-job2:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "This is Step 1 of Reusable Workflow 2"

      - name: Step 2
        run: echo "This is Step 2 of Reusable Workflow 2"
